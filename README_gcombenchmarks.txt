#To run benchmarks, make sure GCOM is built and installed in modelPerf, chmod +x whatever benchmark file you are trying to run and run it. May need to edit paths on your system, but once that is done, it should run completely all the way through, install all dependences etc, and output gcom logs. 
#I used a virutal environment, I reccomend you also do the same. 
#Just in case, run pip install onnxruntime-gpu before starting in the venv
